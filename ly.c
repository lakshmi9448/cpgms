#include<stdio.h>
 void main()
  { int a;
    printf("enter a year to check if its a leap year");
    scanf("%d",&a);
    if(a%4==0)
    {
      printf("its a leap year");
    }
    else
    printf("not a leap year");
  }
    
